/**
 * End-to-End Payment Flow Integration Test
 * Tests the complete customer journey from cart to successful order
 */

import { supabaseAdmin } from '../../src/config/supabase.js';
import stripe from '../../src/config/stripe.js';
import '../../src/config/env.js';
import { sendOrderConfirmationEmail, sendAdminOrderNotification } from '../../src/services/emailService.js';

// Test configuration
const TEST_CONFIG = {
  // Use existing test user with cart and addresses
  testUserId: '4debb088-7396-4c8d-9df0-919c87615fe3',
  testEmail: 'yuliacevedo05@gmail.com',
  testProductId: 10, // Café Mont Rose product
  testQuantity: 1
};

class PaymentFlowTester {
  constructor() {
    this.testResults = [];
    this.originalStock = {};
    this.createdOrderId = null;
    this.paymentIntentId = null;
  }

  log(step, status, message, data = null) {
    const result = {
      step,
      status,
      message,
      timestamp: new Date().toISOString(),
      data
    };
    this.testResults.push(result);
    
    const emoji = status === 'SUCCESS' ? '✅' : status === 'ERROR' ? '❌' : 'ℹ️';
    console.log(`${emoji} ${step}: ${message}`);\n    if (data) {\n      console.log('   Data:', JSON.stringify(data, null, 2));\n    }\n    console.log('');\n  }\n\n  async runCompleteFlow() {\n    console.log('🚀 Starting Complete E-commerce Payment Flow Test\\n');\n    \n    try {\n      // Step 1: Setup and validation\n      await this.validateTestEnvironment();\n      \n      // Step 2: Cart operations\n      await this.testCartOperations();\n      \n      // Step 3: Cart total calculations\n      await this.testCartTotalCalculations();\n      \n      // Step 4: Payment intent creation\n      await this.testPaymentIntentCreation();\n      \n      // Step 5: Simulate successful payment\n      await this.simulateSuccessfulPayment();\n      \n      // Step 6: Order creation and processing\n      await this.testOrderCreationAndProcessing();\n      \n      // Step 7: Email notifications\n      await this.testEmailNotifications();\n      \n      // Step 8: Database verification\n      await this.verifyDatabaseUpdates();\n      \n      // Step 9: Cleanup\n      await this.cleanup();\n      \n      this.printSummary();\n      \n    } catch (error) {\n      this.log('FATAL_ERROR', 'ERROR', 'Test flow failed', { error: error.message });\n      throw error;\n    }\n  }\n\n  async validateTestEnvironment() {\n    this.log('ENVIRONMENT', 'INFO', 'Validating test environment');\n    \n    // Check database connection\n    const { data: testUser, error: userError } = await supabaseAdmin\n      .from('usuarios')\n      .select('id, correo_electronico, nombre')\n      .eq('id', TEST_CONFIG.testUserId)\n      .single();\n    \n    if (userError || !testUser) {\n      throw new Error('Test user not found in database');\n    }\n    \n    this.log('ENVIRONMENT', 'SUCCESS', 'Test user validated', {\n      user: testUser.correo_electronico\n    });\n    \n    // Check Stripe configuration\n    if (!process.env.STRIPE_SECRET_KEY || !process.env.STRIPE_SECRET_KEY.startsWith('sk_')) {\n      throw new Error('Invalid Stripe secret key configuration');\n    }\n    \n    this.log('ENVIRONMENT', 'SUCCESS', 'Stripe configuration validated');\n    \n    // Check email configuration\n    if (!process.env.RESEND_API_KEY) {\n      throw new Error('Resend API key not configured');\n    }\n    \n    this.log('ENVIRONMENT', 'SUCCESS', 'Email service configuration validated');\n  }\n\n  async testCartOperations() {\n    this.log('CART_OPS', 'INFO', 'Testing cart operations');\n    \n    // Get current product stock\n    const { data: product, error: productError } = await supabaseAdmin\n      .from('productos')\n      .select('id, nombre, precio, stock, \"TPS\", \"TVQ\", consigne')\n      .eq('id', TEST_CONFIG.testProductId)\n      .single();\n    \n    if (productError || !product) {\n      throw new Error('Test product not found');\n    }\n    \n    this.originalStock[product.id] = product.stock;\n    \n    this.log('CART_OPS', 'SUCCESS', 'Product data retrieved', {\n      product: product.nombre,\n      originalStock: product.stock,\n      price: product.precio\n    });\n    \n    // Clear existing cart items for clean test\n    await supabaseAdmin\n      .from('carrito')\n      .delete()\n      .eq('usuario_id', TEST_CONFIG.testUserId);\n    \n    // Add test item to cart\n    const { data: cartItem, error: cartError } = await supabaseAdmin\n      .from('carrito')\n      .insert({\n        usuario_id: TEST_CONFIG.testUserId,\n        producto_id: TEST_CONFIG.testProductId,\n        cantidad: TEST_CONFIG.testQuantity\n      })\n      .select()\n      .single();\n    \n    if (cartError) {\n      throw new Error(`Failed to add item to cart: ${cartError.message}`);\n    }\n    \n    this.log('CART_OPS', 'SUCCESS', 'Item added to cart successfully', {\n      cartItemId: cartItem.id,\n      quantity: cartItem.cantidad\n    });\n  }\n\n  async testCartTotalCalculations() {\n    this.log('CART_CALC', 'INFO', 'Testing cart total calculations');\n    \n    // Get cart with product details\n    const { data: cartItems, error: cartError } = await supabaseAdmin\n      .from('carrito')\n      .select(`\n        *,\n        productos(\n          id, nombre, precio, stock, \"TPS\", \"TVQ\", consigne\n        )\n      `)\n      .eq('usuario_id', TEST_CONFIG.testUserId);\n    \n    if (cartError || !cartItems || cartItems.length === 0) {\n      throw new Error('Cart is empty or could not be retrieved');\n    }\n    \n    // Calculate totals manually\n    let subtotal = 0;\n    let totalTPS = 0;\n    let totalTVQ = 0;\n    let totalConsigne = 0;\n    \n    const calculations = cartItems.map(item => {\n      const product = item.productos;\n      const itemSubtotal = parseFloat(product.precio) * item.cantidad;\n      const itemTPS = product.TPS ? (itemSubtotal * (parseFloat(product.TPS) / 100)) : 0;\n      const itemTVQ = product.TVQ ? (itemSubtotal * (parseFloat(product.TVQ) / 100)) : 0;\n      const itemConsigne = product.consigne ? (parseFloat(product.consigne) * item.cantidad) : 0;\n      \n      subtotal += itemSubtotal;\n      totalTPS += itemTPS;\n      totalTVQ += itemTVQ;\n      totalConsigne += itemConsigne;\n      \n      return {\n        product: product.nombre,\n        quantity: item.cantidad,\n        unitPrice: parseFloat(product.precio),\n        subtotal: itemSubtotal,\n        tps: itemTPS,\n        tvq: itemTVQ,\n        consigne: itemConsigne\n      };\n    });\n    \n    const freeShippingThreshold = 200.00;\n    const baseShipping = 9.99;\n    const shippingCost = subtotal >= freeShippingThreshold ? 0 : baseShipping;\n    const totalAmount = subtotal + totalTPS + totalTVQ + totalConsigne + shippingCost;\n    \n    this.log('CART_CALC', 'SUCCESS', 'Cart calculations completed', {\n      items: calculations,\n      summary: {\n        subtotal: subtotal.toFixed(2),\n        tps: totalTPS.toFixed(2),\n        tvq: totalTVQ.toFixed(2),\n        consigne: totalConsigne.toFixed(2),\n        shipping: shippingCost.toFixed(2),\n        total: totalAmount.toFixed(2),\n        freeShipping: subtotal >= freeShippingThreshold\n      }\n    });\n    \n    // Store for later verification\n    this.expectedTotal = totalAmount;\n  }\n\n  async testPaymentIntentCreation() {\n    this.log('PAYMENT_INTENT', 'INFO', 'Testing payment intent creation');\n    \n    // Get test address\n    const { data: address, error: addressError } = await supabaseAdmin\n      .from('direcciones_envio')\n      .select('*')\n      .eq('usuario_id', TEST_CONFIG.testUserId)\n      .limit(1)\n      .single();\n    \n    if (addressError || !address) {\n      throw new Error('Test address not found');\n    }\n    \n    // Test the payment intent creation logic (without actually calling Stripe)\n    const testPaymentData = {\n      shipping_address_id: address.id,\n      coupon_code: null\n    };\n    \n    this.log('PAYMENT_INTENT', 'SUCCESS', 'Payment intent parameters prepared', {\n      addressId: address.id,\n      address: `${address.direccion}, ${address.ciudad}`,\n      expectedTotal: this.expectedTotal\n    });\n    \n    // For testing, we'll create a mock payment intent ID\n    this.paymentIntentId = `pi_test_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;\n    \n    this.log('PAYMENT_INTENT', 'SUCCESS', 'Mock payment intent created', {\n      paymentIntentId: this.paymentIntentId\n    });\n  }\n\n  async simulateSuccessfulPayment() {\n    this.log('PAYMENT_SIM', 'INFO', 'Simulating successful payment');\n    \n    // In a real test, this would involve Stripe test cards\n    // For now, we'll simulate a successful payment status\n    \n    const mockPaymentResult = {\n      id: this.paymentIntentId,\n      status: 'succeeded',\n      amount: Math.round(this.expectedTotal * 100), // Convert to cents\n      currency: 'cad',\n      created: Math.floor(Date.now() / 1000)\n    };\n    \n    this.log('PAYMENT_SIM', 'SUCCESS', 'Payment simulation completed', {\n      paymentResult: mockPaymentResult\n    });\n  }\n\n  async testOrderCreationAndProcessing() {\n    this.log('ORDER_CREATE', 'INFO', 'Testing order creation and processing');\n    \n    // Get cart items for order creation\n    const { data: cartItems, error: cartError } = await supabaseAdmin\n      .from('carrito')\n      .select(`\n        *,\n        productos(id, nombre, precio, stock)\n      `)\n      .eq('usuario_id', TEST_CONFIG.testUserId);\n    \n    if (cartError || !cartItems || cartItems.length === 0) {\n      throw new Error('Cart is empty for order creation');\n    }\n    \n    // Get shipping address\n    const { data: address } = await supabaseAdmin\n      .from('direcciones_envio')\n      .select('*')\n      .eq('usuario_id', TEST_CONFIG.testUserId)\n      .limit(1)\n      .single();\n    \n    // Create order\n    const { data: order, error: orderError } = await supabaseAdmin\n      .from('pedidos')\n      .insert({\n        usuario_id: TEST_CONFIG.testUserId,\n        total: this.expectedTotal,\n        estado: 'confirmado',\n        stripe_payment_intent_id: this.paymentIntentId,\n        direccion_envio_id: address?.id || null,\n        subtotal: this.expectedTotal - 9.99, // Rough calculation for test\n        impuestos_tps: 0,\n        impuestos_tvq: 0,\n        costos_envio: this.expectedTotal >= 200 ? 0 : 9.99,\n        descuento: 0\n      })\n      .select()\n      .single();\n    \n    if (orderError) {\n      throw new Error(`Failed to create order: ${orderError.message}`);\n    }\n    \n    this.createdOrderId = order.id;\n    \n    this.log('ORDER_CREATE', 'SUCCESS', 'Order created successfully', {\n      orderId: order.id,\n      total: order.total,\n      status: order.estado\n    });\n    \n    // Create order details\n    const orderDetails = cartItems.map(item => ({\n      pedido_id: order.id,\n      producto_id: item.productos.id,\n      cantidad: item.cantidad,\n      precio_unitario: parseFloat(item.productos.precio)\n    }));\n    \n    const { error: detailsError } = await supabaseAdmin\n      .from('detalles_pedido')\n      .insert(orderDetails);\n    \n    if (detailsError) {\n      throw new Error(`Failed to create order details: ${detailsError.message}`);\n    }\n    \n    this.log('ORDER_CREATE', 'SUCCESS', 'Order details created successfully', {\n      itemCount: orderDetails.length\n    });\n    \n    // Update product stock\n    for (const item of cartItems) {\n      const newStock = item.productos.stock - item.cantidad;\n      const { error: stockError } = await supabaseAdmin\n        .from('productos')\n        .update({ stock: newStock })\n        .eq('id', item.productos.id);\n      \n      if (stockError) {\n        console.error('Stock update error:', stockError);\n      } else {\n        this.log('ORDER_CREATE', 'SUCCESS', 'Stock updated', {\n          productId: item.productos.id,\n          oldStock: item.productos.stock,\n          newStock: newStock,\n          quantity: item.cantidad\n        });\n      }\n    }\n    \n    // Clear cart\n    const { error: clearCartError } = await supabaseAdmin\n      .from('carrito')\n      .delete()\n      .eq('usuario_id', TEST_CONFIG.testUserId);\n    \n    if (clearCartError) {\n      console.error('Clear cart error:', clearCartError);\n    } else {\n      this.log('ORDER_CREATE', 'SUCCESS', 'Cart cleared successfully');\n    }\n  }\n\n  async testEmailNotifications() {\n    this.log('EMAIL_TEST', 'INFO', 'Testing email notifications');\n    \n    if (!this.createdOrderId) {\n      throw new Error('No order ID available for email testing');\n    }\n    \n    // Test customer confirmation email\n    try {\n      const customerEmailResult = await sendOrderConfirmationEmail(this.createdOrderId);\n      \n      if (customerEmailResult.success) {\n        this.log('EMAIL_TEST', 'SUCCESS', 'Customer confirmation email sent', {\n          emailId: customerEmailResult.emailId\n        });\n      } else {\n        this.log('EMAIL_TEST', 'ERROR', 'Customer email failed', {\n          error: customerEmailResult.error\n        });\n      }\n    } catch (error) {\n      this.log('EMAIL_TEST', 'ERROR', 'Customer email exception', {\n        error: error.message\n      });\n    }\n    \n    // Test admin notification email\n    try {\n      const adminEmailResult = await sendAdminOrderNotification(this.createdOrderId);\n      \n      if (adminEmailResult.success) {\n        this.log('EMAIL_TEST', 'SUCCESS', 'Admin notification email sent', {\n          emailId: adminEmailResult.emailId\n        });\n      } else {\n        this.log('EMAIL_TEST', 'ERROR', 'Admin email failed', {\n          error: adminEmailResult.error\n        });\n      }\n    } catch (error) {\n      this.log('EMAIL_TEST', 'ERROR', 'Admin email exception', {\n        error: error.message\n      });\n    }\n  }\n\n  async verifyDatabaseUpdates() {\n    this.log('DB_VERIFY', 'INFO', 'Verifying database updates');\n    \n    // Verify order exists\n    const { data: order, error: orderError } = await supabaseAdmin\n      .from('pedidos')\n      .select(`\n        *,\n        detalles_pedido(*)\n      `)\n      .eq('id', this.createdOrderId)\n      .single();\n    \n    if (orderError || !order) {\n      throw new Error('Created order not found in database');\n    }\n    \n    this.log('DB_VERIFY', 'SUCCESS', 'Order verified in database', {\n      orderId: order.id,\n      itemCount: order.detalles_pedido.length,\n      status: order.estado\n    });\n    \n    // Verify stock updates\n    const { data: product, error: productError } = await supabaseAdmin\n      .from('productos')\n      .select('stock')\n      .eq('id', TEST_CONFIG.testProductId)\n      .single();\n    \n    if (!productError && product) {\n      const expectedStock = this.originalStock[TEST_CONFIG.testProductId] - TEST_CONFIG.testQuantity;\n      \n      if (product.stock === expectedStock) {\n        this.log('DB_VERIFY', 'SUCCESS', 'Product stock correctly updated', {\n          productId: TEST_CONFIG.testProductId,\n          originalStock: this.originalStock[TEST_CONFIG.testProductId],\n          currentStock: product.stock,\n          quantityOrdered: TEST_CONFIG.testQuantity\n        });\n      } else {\n        this.log('DB_VERIFY', 'ERROR', 'Stock update mismatch', {\n          expected: expectedStock,\n          actual: product.stock\n        });\n      }\n    }\n    \n    // Verify cart is empty\n    const { data: cartItems, error: cartError } = await supabaseAdmin\n      .from('carrito')\n      .select('*')\n      .eq('usuario_id', TEST_CONFIG.testUserId);\n    \n    if (!cartError && cartItems.length === 0) {\n      this.log('DB_VERIFY', 'SUCCESS', 'Cart successfully cleared');\n    } else {\n      this.log('DB_VERIFY', 'ERROR', 'Cart not properly cleared', {\n        remainingItems: cartItems.length\n      });\n    }\n  }\n\n  async cleanup() {\n    this.log('CLEANUP', 'INFO', 'Cleaning up test data');\n    \n    // Restore original stock\n    for (const [productId, originalStock] of Object.entries(this.originalStock)) {\n      await supabaseAdmin\n        .from('productos')\n        .update({ stock: originalStock })\n        .eq('id', productId);\n    }\n    \n    // Note: In a real test environment, you might want to delete the test order\n    // For now, we'll leave it for manual verification\n    \n    this.log('CLEANUP', 'SUCCESS', 'Cleanup completed');\n  }\n\n  printSummary() {\n    console.log('\\n' + '='.repeat(80));\n    console.log('📊 E-COMMERCE PAYMENT FLOW TEST SUMMARY');\n    console.log('='.repeat(80));\n    \n    const successCount = this.testResults.filter(r => r.status === 'SUCCESS').length;\n    const errorCount = this.testResults.filter(r => r.status === 'ERROR').length;\n    const totalSteps = this.testResults.length;\n    \n    console.log(`\\n🎯 Results: ${successCount}/${totalSteps} steps successful`);\n    console.log(`✅ Successful: ${successCount}`);\n    console.log(`❌ Errors: ${errorCount}`);\n    \n    if (this.createdOrderId) {\n      console.log(`\\n📦 Created Order ID: ${this.createdOrderId}`);\n    }\n    \n    if (errorCount > 0) {\n      console.log('\\n❌ Errors encountered:');\n      this.testResults\n        .filter(r => r.status === 'ERROR')\n        .forEach(r => console.log(`  - ${r.step}: ${r.message}`));\n    }\n    \n    console.log('\\n' + '='.repeat(80));\n    console.log(errorCount === 0 ? '🎉 ALL TESTS PASSED!' : '⚠️  SOME TESTS FAILED');\n    console.log('='.repeat(80) + '\\n');\n  }\n}\n\n// Export for use as module or run directly\nexport default PaymentFlowTester;\n\n// Run test if executed directly\nif (import.meta.url === `file://${process.argv[1]}`) {\n  const tester = new PaymentFlowTester();\n  tester.runCompleteFlow()\n    .then(() => {\n      console.log('\\n✨ Test execution completed!');\n      process.exit(0);\n    })\n    .catch(error => {\n      console.error('\\n💥 Test execution failed:', error);\n      process.exit(1);\n    });\n}